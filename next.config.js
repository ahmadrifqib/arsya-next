/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	// images: {
	// 	loader: "custom",
	// 	path: "https://localhost:3000/",
	// },
};

module.exports = nextConfig;
